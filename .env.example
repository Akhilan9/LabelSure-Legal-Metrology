# Local Docker development only. Choose a password before running Compose.
POSTGRES_USER=labelsure
POSTGRES_PASSWORD=labelsure_local_change_me
POSTGRES_DB=labelsure
# Set a generated random JWT_SECRET of at least 32 bytes in local .env.
# JWT_SECRET=
